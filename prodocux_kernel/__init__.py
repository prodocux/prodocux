"""ProDocuX Kernel — deterministic document processing engine.

The runtime does not call any LLM API (see CONTRACT.md §1.1).
"""

__version__ = "0.3.0rc4"
API_VERSION = "v1"
FROZEN_MODEL = "solver-side (kernel makes no LLM calls)"
